# 0→1 实现文档：⚡「优化提示词」按钮（v2 · 已落地版）

> 目标：在会话输入框旁放一个 ⚡（`auto_awesome`）图标按钮，点击后调用 AI 模型把输入框里的提示词改写得 更清晰、更具体，再回填到输入框。
>
> **v2 说明**：v1 文档的实现思路有 4 处会导致「点击没反应 / 报错」，已在本仓库按修正版落地。§2 列出根因，照抄时请直接跳到根因之后的内容。

---

## 1. 功能定义

| 项 | 说明 |
|---|---|
| 入口 | 会话输入框（composer）工具栏，圆发送按钮**左侧**；`auto_awesome` 图标 |
| 交互 | 输入框有内容 → 点击 ⚡ → 按钮进入加载态（Spinner + 禁用）→ 模型返回 → 回填输入框并把光标移到末尾 |
| 规则 | 保留原意图、补充模糊点、消除歧义、多部分请求结构化、**保持用户原语言**、只输出改写后的文本 |
| 失败 | 弹 toast 错误，输入框内容不变 |

⚠️ **作用范围**：本项目有**两个互相独立的 composer**——`ChatInput.tsx`（会话中）和 `EmptySession.tsx`（首页空会话）。当前只加在 `ChatInput.tsx`。

---

## 2. 与 v1 的差异（4 条「照抄不生效」根因）

这四条是 v1 文档最贵的地方，逐条说明现象与原因。

### 根因 1：自己 `new Anthropic({ baseURL, apiKey })` 调不通 —— 主因

v1 的写法：

```ts
// ❌ 只对「apiFormat: 'anthropic' + api_key 鉴权」的服务商有效
const client = new Anthropic({ baseURL, apiKey, authToken, dangerouslyAllowBrowser: true })
await client.beta.messages.create({ model, max_tokens, messages })
```

在下面几类服务商上必然失败：

| 服务商类型 | 现象 |
|---|---|
| OpenAI 格式中转/预设（`apiFormat: 'openai_chat'` / `'openai_responses'`，如 opencode-go、DeepSeek 类中转） | `POST /v1/messages` 被网关直接拒绝（404 或 400），需要走 `/chat/completions` 或 `/responses` |
| OpenAI 官方 OAuth 服务商 | 需要 ChatGPT OAuth token + Codex 协议，401 |
| `authStrategy` 为 `dual_same_token` / `dual_dummy` 的网关 | 401 —— v1 只处理了 `x-api-key` 一种 |
| 模型名 | v1 回退到硬编码 `'claude-haiku-4-5-20251001'`，用户的服务商根本没有这个模型 → 400 model not found |
| `client.beta.*` | 部分第三方网关不实现 beta 命名空间 → 404 |

**正确做法**：复用 `src/server/services/providerCompletion.ts`（本项目已抽出）。它会：

1. 用 `ProviderService.listProviders()` 解析活动服务商（或指定 `providerId`）；
2. 用 `providerNeedsProxy(resolveProviderApiFormat(p), p.supportsNestedToolResultMedia)` 判断是**直连** `/v1/messages` 还是必须**走进程内代理** `http://127.0.0.1/proxy/providers/<id>/v1/messages`（代理负责 OpenAI ↔ Anthropic 协议转换与预设专属请求头）；
3. 处理全部 5 种 `authStrategy` 的请求头映射；
4. 处理 OpenAI 官方 OAuth 路径；
5. 直连路径第一发带 `thinking: { type: 'disabled' }`，遇 4xx 去掉 `thinking` 重试一次；
6. 任何失败返回 `null`，不抛。

> 这段逻辑原本只存在于 `titleService.ts`（会话标题生成）。两个功能都要「调活动服务商的小模型」，所以抽成了公共模块；`titleService` 也改成调用它。

### 根因 2：i18n 只加 en + zh 会编译失败

`en.ts` 导出 `type TranslationKey = keyof typeof en`，而 `zh.ts` / `zh-TW.ts` / `jp.ts` / `kr.ts` 都是：

```ts
export const zh: Record<TranslationKey, string> = { ... }
```

`Record<TranslationKey, string>` 是**穷尽**的——**5 个语言包必须都加**，少一个 `tsc --noEmit` 直接报错。

### 根因 3：回填时不清空 mentions，文本会错乱

v1 写的是 `setComposerInput(result.optimized)`。本项目输入框是 ProseMirror 驱动的 `MentionComposer`，第二个参数是 mention pill 的区间列表。只改文本、保留旧 mentions，等于把**旧的 pill 区间套到一段无关的新文本上**，渲染直接错位。

正确写法必须传空数组：

```ts
setComposerInput(optimized, [])
```

### 根因 4：超时取值

v1 写「前端 30s / 后端 600s」。改写一段完整提示词属于长输出，30s 对慢模型/中转偏紧，表现为「转圈半天然后前端先报网络错误」，而后端其实还在跑。

本项目的取值：**后端 60s，前端 90s**（前端必须大于后端，否则后端还在跑、前端已经断了）。

### 附带修正

- 文件路径：v1 若引用目录选择器，实际路径是 `desktop/src/components/composite/DirectoryPicker.tsx`，**不是** `shared/`。
- v1 的 `readActiveProviderManagedEnv(configDir)` 读 `<configDir>/cc-haha/providers.json` 是 CLI 侧的托管环境变量读取，不适合服务端 API；服务端请用 `ProviderService`。
- v1 没有定义任何错误码，前端只能拿到一个笼统的 500。见 §4.2 的错误码表。

---

## 3. 实现步骤总览

| 步骤 | 位置 | 做什么 |
|---|---|---|
| 1 | `src/server/services/providerCompletion.ts` | 后端：**复用**——服务商解析 + 一次性文本生成 |
| 2 | `src/server/api/optimize.ts` | 后端：优化 API 主体 |
| 3 | `src/server/router.ts` | 后端：注册 `POST /api/optimize` |
| 4 | `desktop/src/api/sessions.ts` | 前端：`sessionsApi.optimizePrompt` |
| 5 | `desktop/src/components/chat/ChatInput.tsx` | 前端：⚡ 按钮 + 点击逻辑 |
| 6 | `desktop/src/i18n/locales/{en,zh,zh-TW,jp,kr}.ts` | 前端：文案键（**5 个文件都要**） |
| 7 | `src/server/__tests__/optimize-api.test.ts`、`desktop/src/components/chat/ChatInput.test.tsx` | 测试 |

---

## 4. 后端实现

### 4.1 复用 `src/server/services/providerCompletion.ts`

对外只有两个函数：

```ts
export type ResolvedCompletionProvider =
  | { kind: 'openai-official'; model: string }
  | {
      kind: 'anthropic'
      id: string
      baseUrl: string
      apiKey: string
      authStrategy: ProviderAuthStrategy
      usesLocalProxy: boolean
      model: string
    }

/** 不传 providerId = 用活动服务商；显式传 null = 调用方决定不生成，返回 null */
export async function resolveCompletionProvider(
  providerId?: string | null,
): Promise<ResolvedCompletionProvider | null>

/** 返回模型输出的纯文本；任何失败返回 null（不抛） */
export async function completeWithProvider(
  resolved: ResolvedCompletionProvider,
  request: {
    system: string
    userContent: string
    maxTokens: number
    sessionId?: string   // 透传给代理，网关据此路由
    timeoutMs?: number   // 默认 15_000
  },
): Promise<string | null>
```

**在新项目里落地**：如果新项目已有「解析活动服务商」的能力，只要保证这两个函数能拿到 `baseUrl / apiKey / authStrategy / apiFormat / models.haiku` 即可，不必照搬实现。

### 4.2 优化 API 主体 —— `src/server/api/optimize.ts`

```ts
export async function handleOptimizeApi(
  req: Request,
  _url: URL,
  _segments: string[],
): Promise<Response>
```

约定（与 `src/server/api/models.ts` 一致）：

- 路由层**不检查 method**，handler 自己判，否则抛 `405 METHOD_NOT_ALLOWED`；
- 顶层 `try/catch` + `errorResponse(error)`；
- 用全局 `Response.json(...)`，没有本地 `json()` helper；
- 请求体用 `await req.json()`，包一层 `parseJsonBody` 把解析失败转成 400。

系统提示词：

```
You are a prompt optimization assistant. Your task is to improve the user's prompt to make it clearer, more specific, and more likely to produce high-quality results.

Rules:
1. Preserve the original intent — do NOT change what the user is asking for
2. Add necessary context and specificity if the prompt is vague
3. Remove ambiguity and clarify any unclear requests
4. Structure the prompt logically (e.g., numbered steps for multi-part requests)
5. Keep the optimized prompt concise — only add what's truly needed
6. Stay in the language the user wrote in
7. Output ONLY the optimized prompt text — no explanations, no markdown fences, no prefixes
```

用户消息把原提示词包起来，避免模型把提示词内容当成指令：

```
Improve the following prompt. Make it clearer, more specific, and more detailed while preserving the original intent.

Output ONLY the improved prompt text — no explanations or prefixes.

<prompt>
{用户输入}
</prompt>
```

常量：`MAX_PROMPT_LENGTH = 8000`、`OPTIMIZE_MAX_OUTPUT_TOKENS = 4096`、`OPTIMIZE_TIMEOUT_MS = 60_000`。

错误码表（**前端据此区分「去配置」「重试」和「上游故障」**）：

| 情况 | 状态码 | `error` |
|---|---|---|
| 非 POST | 405 | `METHOD_NOT_ALLOWED` |
| body 非 JSON / `prompt` 非字符串或为空 | 400 | `BAD_REQUEST` |
| `prompt.length > 8000` | 400 | `BAD_REQUEST` |
| 解析不到活动服务商 | 409 | `NO_ACTIVE_PROVIDER` |
| 上游返回非 2xx | 502 | `OPTIMIZE_UPSTREAM_ERROR` |
| 上游 2xx 但没有 text block / body 不是 JSON | 502 | `OPTIMIZE_EMPTY_RESPONSE` |
| 抛异常（网络、超时、DNS、OAuth 缺 token） | 502 | `OPTIMIZE_FAILED` |
| 成功 | 200 | `{ optimized: string }`（已 trim） |

### ⚠️ 不要把所有失败压成一个 `null`

v2 初版让 `completeWithProvider` 在失败时返回 `null`，于是 401、400、超时、「模型只回了 thinking」全都变成同一句
「The provider did not return an optimized prompt」——用户被指去检查本来没问题的设置，排查直接卡死。

**正确做法**：返回带失败详情的判别联合，并把上游的 status + body 摘要带出来。

```ts
export type CompletionFailure =
  | { kind: 'http'; status: number; detail?: string }       // 上游非 2xx
  | { kind: 'no-text'; status: number; detail?: string }    // 2xx 但没有 text block
  | { kind: 'exception'; detail?: string }                  // 抛异常

export type CompletionResult =
  | { ok: true; text: string }
  | { ok: false; failure: CompletionFailure }
```

配套要点：

- `readAnthropicText` **先 `response.text()` 再 `JSON.parse`**，不要直接 `.json()`。直接 `.json()` 会丢掉原始 body，遇到 SSE 或非 JSON 只能报「无内容」；先拿原文才能在 message 里写明 `unparsable body: data: {...}`。
- 没有 text block 时，把实际拿到的 block 类型列出来（`content blocks: thinking`）——这一条就能直接判定「推理模型只回思考过程」。
- 服务端 `console.error('[Optimize] provider call failed', { provider, failure })`，便于看日志。
- `detail` 用 `truncate()` 压平空白并截断到 300 字符，避免上游 HTML 错误页刷屏。
- `titleService` 只关心成功与否，读 `result.ok` 即可（标题是 fire-and-forget，失败就保留占位标题）。

排查路径：拿到 message 后

| message 里出现 | 结论 |
|---|---|
| `HTTP 401` / `invalid x-api-key` | 服务商密钥或 `authStrategy` 不对 |
| `HTTP 404` / `model not found` | `models.haiku` 指向的模型该服务商没有 |
| `HTTP 400` / `max_tokens` | 该模型输出上限小于 4096，调小 `OPTIMIZE_MAX_OUTPUT_TOKENS` |
| `content blocks: thinking` | 活动服务商选到推理模型，换一个非推理的 haiku 档模型 |
| `unparsable body: data:` | 网关强制返回 SSE，需要走代理路径或按 SSE 解析 |

### 4.3 路由注册 —— `src/server/router.ts`

`resource = segments[1]`，所以 `/api/optimize` 命中 `'optimize'`：

```ts
import { handleOptimizeApi } from './api/optimize.js'

// switch (resource) 内，与其它 case 平级
case 'optimize':
  return handleOptimizeApi(req, url, segments)
```

---

## 5. 前端实现

### 5.1 API 封装 —— `desktop/src/api/sessions.ts`

```ts
optimizePrompt(prompt: string, sessionId?: string) {
  return api.post<{ optimized: string }>(
    '/api/optimize',
    { prompt, sessionId },
    { timeout: 90_000 },   // 必须大于后端的 60s
  )
},
```

### 5.2 ⚡ 按钮 + 点击逻辑 —— `desktop/src/components/chat/ChatInput.tsx`

**状态**：`const [optimizeLoading, setOptimizeLoading] = useState(false)`

**点击逻辑**：

```ts
const handleOptimize = useCallback(async () => {
  if (optimizeLoading) return
  // 从实时文档读取，mention pill 会被序列化成模型该看到的路径
  const text = (composerRef.current?.getModelContent() ?? inputRef.current).trim()
  if (!text) return

  setOptimizeLoading(true)
  try {
    const { optimized } = await sessionsApi.optimizePrompt(text, activeTabId ?? undefined)
    if (!optimized || optimized === text) return

    setComposerInput(optimized, [])          // ← 必须传 []，见根因 3
    requestAnimationFrame(() => {
      composerRef.current?.focus()
      composerRef.current?.setSelectionOffsets(optimized.length)
    })
  } catch (error) {
    useUIStore.getState().addToast({
      type: 'error',
      message: error instanceof Error ? error.message : t('chat.optimizePromptFailed'),
    })
  } finally {
    setOptimizeLoading(false)
  }
}, [activeTabId, optimizeLoading, setComposerInput, t])
```

**按钮 JSX**（插在圆发送按钮 `<Button shape="circle">` **之前**）：

```tsx
{!isMemberSession && (
  <IconButton
    icon="auto_awesome"
    label={t('chat.optimizePrompt')}
    onClick={() => { void handleOptimize() }}
    disabled={!input.trim() || optimizeLoading}
    loading={optimizeLoading}
    size={isMobileComposer ? '2xl' : 'md'}   // 44px / 32px，与相邻发送按钮同档
    tone="muted"
    className="shrink-0"
  />
)}
```

要点：

- 用项目已有的 `IconButton`（`desktop/src/components/ui/IconButton.tsx`）。它的 `loading` 属性自带 Spinner + `disabled` + `aria-busy`，**不要自己写旋转图标**。
- 尺寸档位 `md` = 32px、`2xl` = 44px，正好对齐同排的发送按钮（`isMobileComposer ? 'h-11 w-11' : 'h-8 w-8'`）。
- 加载态是**必要的**：改写要跑几秒，只有一个「点了没反应」的按钮会被当成坏了。
- `getModelContent()` 会把 mention pill 序列化成路径——那才是该发给模型的内容。写入时 pill 会被替换成纯文本，这是预期行为。

### 5.3 i18n 文案 —— 5 个语言包都要加

| key | en | zh | zh-TW | jp | kr |
|---|---|---|---|---|---|
| `chat.optimizePrompt` | `Optimize prompt` | `优化提示词` | `最佳化提示詞` | `プロンプトを最適化` | `프롬프트 최적화` |
| `chat.optimizePromptFailed` | `Failed to optimize prompt` | `提示词优化失败` | `提示詞最佳化失敗` | `プロンプトの最適化に失敗しました` | `프롬프트 최적화 실패` |

放在 `chat.*` 段内（本项目在 `chat.composerTools` 之后）。

---

## 6. 测试

### 后端 `src/server/__tests__/optimize-api.test.ts`

打桩方式沿用 `title-service.test.ts`：

- `process.env.CLAUDE_CONFIG_DIR` 指向临时目录，`new ProviderService().addProvider({...})` + `activateProvider(id)`；
- 用 `Bun.serve({ hostname: '127.0.0.1', port: 0, fetch })` 返回 `Response.json({ content: [{ type: 'text', text: '...' }] })`，并断言收到的 `model` / `system` / `messages`；
- OpenAI 格式走代理的用例：替换 `globalThis.fetch` 后断言落在 `/chat/completions`、带 `x-opencode-session`；
- 覆盖：405、空/超长 prompt 400、无服务商 409、上游 500 → 502、无 text block → 502、非法 JSON 400。

> **Windows 清理坑**：代理相关用例结束后，后台 trace projection 会短暂锁住临时目录，`fs.rm` 报 `EBUSY` 让测试假失败。照 `src/server/__tests__/proxy-anthropic-compat.test.ts` 的 teardown 写：`resetSettingsCache()` → `drainTraceCaptureForTests()` → `clearTraceCaptureStateForTests()` → 重试 `fs.rm` 最多 10 次。

### 前端 `desktop/src/components/chat/ChatInput.test.tsx`

该文件用 `vi.mock('../../api/sessions', ...)` **整体替换模块**——mock 对象里没列出的方法运行时就是 `undefined`。所以新增 `optimizePrompt` 必须同时改三处：

1. `vi.hoisted` 块加 `optimizePrompt: vi.fn()`
2. `vi.mock` 的对象里加 `optimizePrompt: mocks.optimizePrompt`
3. `beforeEach` 里 `mocks.optimizePrompt.mockResolvedValue({ optimized: '...' })`

用例：回填成功、改写结果与原文相同则不动、空输入禁用、失败弹 toast 且保留原文。

---

## 7. 验证清单

```bash
# 1. 依赖
bun install && cd desktop && bun install

# 2. 后端测试
bun test src/server/__tests__/optimize-api.test.ts

# 3. 前端 lint（eslint + tsc --noEmit）+ 测试
cd desktop && bun run lint && bun run test -- --run src/components/chat/ChatInput.test.tsx
```

| # | 操作 | 预期 |
|---|---|---|
| 1 | 配置并激活服务商 | — |
| 2 | 输入框写「写个登录功能」 | ⚡ 可点击 |
| 3 | 点击 ⚡ | 按钮变 Spinner 且禁用 |
| 4 | 等待返回 | 输入框被替换为更具体的提示词，光标在末尾 |
| 5 | **用 OpenAI 格式中转（DeepSeek 等）重复 3–4** | 同样成功（这是 v1 会失败的场景） |
| 6 | 输入框为空时看按钮 | 禁用 |
| 7 | 未配置服务商时点击 | toast：未配置服务商；输入框不变 |
| 8 | 断网 / 服务商地址错误时点击 | toast 报错（而不是静默无反应） |
| 9 | 输入框里有 @ 文件 pill 时点击 | 模型收到路径文本；回填后 pill 变成纯文本 |

---

## 8. 常见问题

| 问题 | 处理 |
|---|---|
| 点击后转圈很久然后报网络错误 | 前端超时短于后端。前端 90s > 后端 60s |
| 502 且 message 是笼统的「没返回提示词」 | 说明失败详情在实现里被丢了，见 §4.2「不要把所有失败压成一个 null」 |
| 502 `OPTIMIZE_UPSTREAM_ERROR` | 上游拒绝，message 里带 HTTP 状态码和上游原文，按上面的排查表定位 |
| 502 `OPTIMIZE_EMPTY_RESPONSE` | 上游返回了，但没有文本。常见于推理模型只回 thinking |
| 返回 409 `NO_ACTIVE_PROVIDER` | 没激活服务商，去设置里选一个 |
| 结果与原文相同 | 前端判断 `optimized !== text` 才回填，避免无意义替换 |
| tsc 报 locale 缺 key | 5 个语言包都要加（根因 2） |
| 回填后文本错位 | 没传 `setComposerInput(text, [])`（根因 3） |

---

## 9. 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | — | 初版：Anthropic SDK 直连 + 只加 en/zh 文案 |
| v2 | 2026-09-19 | 改用 `providerCompletion` 复用服务商调用路径；5 语言包；回填清空 mentions；超时改 60s/90s；补错误码表与测试 |
| v2.1 | 2026-09-19 | `completeWithProvider` 改为返回 `CompletionResult`，带上游 status/body 与 content block 类型；错误码细分为 `OPTIMIZE_UPSTREAM_ERROR` / `OPTIMIZE_EMPTY_RESPONSE`；补排查表 |
