# 0→1 实现文档：首页 ⚡「优化提示词」按钮

> 目标：在一个全新项目中，从零实现「首页会话输入框旁放一个 ⚡ 闪电图标按钮，点击后调用 AI 模型把输入框里的提示词优化得更清晰、更具体，再把结果回填到输入框」。
> 本文按步骤给出完整代码，新项目可逐段复制落地。

---

## 1. 功能定义

| 项 | 说明 |
|---|---|
| 入口 | 会话输入框（composer）发送按钮左侧，`auto_awesome`（闪光/闪电）图标按钮 |
| 交互 | 输入框有内容 → 点击 ⚡ → 按钮进入加载态（旋转图标、禁用）→ 模型返回优化文本 → 回填输入框 |
| 规则 | 保留原意图、补充模糊点、消除歧义、多部分请求结构化、只输出优化文本 |
| 失败 | 弹出错误提示（toast），输入框内容不变 |

## 2. 技术栈与前置条件

- 前端：React + TypeScript（本项目为 Vite + zustand + i18n）
- 后端：Node.js + TypeScript（本项目为原生 `Request`/`Response` 路由，无需 Express 依赖）
- SDK：`@anthropic-ai/sdk`（Anthropic 兼容协议，可接 DeepSeek、Kimi 等中转）
- 前置：项目中已有「服务商管理」能力，能把活动服务商的 baseURL / 密钥 / 模型写入 `providers.json`（见 §6）

## 3. 实现步骤总览

| 步骤 | 位置 | 做什么 |
|---|---|---|
| 1 | `src/server/api/optimize.ts` | 后端：优化 API 主体 |
| 2 | `src/server/router.ts` | 后端：注册 `POST /api/optimize` 路由 |
| 3 | `desktop/src/api/sessions.ts` | 前端：API 封装 |
| 4 | `desktop/src/components/chat/ChatInput.tsx` | 前端：⚡ 按钮 + 点击逻辑 |
| 5 | `desktop/src/i18n/locales/*.ts` | 前端：文案键（中/英等） |

---

## 4. 后端实现

### 4.1 优化 API 主体 —— `src/server/api/optimize.ts`

```ts
/**
 * POST /api/optimize — 优化用户输入的提示词
 * Body: { prompt: string }
 * Response: { optimized: string }
 */

import Anthropic, { APIConnectionError } from '@anthropic-ai/sdk'
import { readActiveProviderManagedEnv } from '../services/providerRuntimeEnv.js'
import { getClaudeConfigHomeDir } from '../../utils/envUtils.js'
import { getProxyFetchOptions } from '../../utils/proxy.js'
import { getUserAgent } from '../../utils/http.js'
import { ApiError, errorResponse } from '../middleware/errorHandler.js'

/** 优化规则：作为 system prompt 注入 */
const OPTIMIZE_SYSTEM_PROMPT = `You are a prompt optimization assistant. Your task is to improve the user's prompt to make it clearer, more specific, and more likely to produce high-quality results.

Rules:
1. Preserve the original intent — do NOT change what the user is asking for
2. Add necessary context and specificity if the prompt is vague
3. Remove ambiguity and clarify any unclear requests
4. Structure the prompt logically (e.g., numbered steps for multi-part requests)
5. Keep the optimized prompt concise — only add what's truly needed
6. Output ONLY the optimized prompt text — no explanations, no markdown fences, no prefixes`

export async function handleOptimizeApi(req: Request): Promise<Response> {
  // 1) 只接受 POST
  if (req.method !== 'POST') {
    throw new ApiError(405, `Method ${req.method} not allowed`, 'METHOD_NOT_ALLOWED')
  }

  // 2) 从服务商配置读取 baseURL / 密钥 / 模型（不依赖 .env，避免代理端口不匹配）
  const configDir = getClaudeConfigHomeDir()
  const managedEnv = readActiveProviderManagedEnv(configDir)
  if (!managedEnv?.ANTHROPIC_BASE_URL) {
    return errorResponse(
      ApiError.internal('No active provider configured. Please set up a provider in Settings.'),
    )
  }

  const baseURL = managedEnv.ANTHROPIC_BASE_URL
  const authToken = managedEnv.ANTHROPIC_AUTH_TOKEN || undefined
  const apiKey = managedEnv.ANTHROPIC_API_KEY || undefined
  const model =
    managedEnv.ANTHROPIC_DEFAULT_HAIKU_MODEL ||
    managedEnv.ANTHROPIC_MODEL ||
    'claude-haiku-4-5-20251001'

  try {
    // 3) 解析请求体
    const body = await req.json() as { prompt?: string }
    if (!body.prompt || typeof body.prompt !== 'string' || body.prompt.trim().length === 0) {
      throw ApiError.badRequest('Missing or empty "prompt" field')
    }

    // 4) 创建 Anthropic 兼容客户端（直连服务商配置的 baseURL）
    const client = new Anthropic({
      baseURL,
      apiKey: apiKey || authToken ? undefined : 'optimize-dummy',
      authToken,
      maxRetries: 1,
      timeout: parseInt(process.env.API_TIMEOUT_MS || String(600 * 1000), 10),
      dangerouslyAllowBrowser: true,
      fetchOptions: getProxyFetchOptions({ forAnthropicAPI: true }) as Record<string, unknown>,
      defaultHeaders: {
        'x-app': 'cli',
        'User-Agent': getUserAgent(),
      },
    })

    // 5) 调用模型
    const response = await client.beta.messages.create({
      model,
      max_tokens: 4096,
      messages: [
        {
          role: 'user',
          content: `Improve the following prompt. Make it clearer, more specific, and more detailed while preserving the original intent.\n\nOutput ONLY the improved prompt text — no explanations or prefixes.\n\nPrompt to improve:\n${body.prompt.trim()}`,
        },
      ],
    })

    // 6) 兼容推理模型（DeepSeek v4 等）：跳过 thinking block，取 text block
    const textContent = response.content.find((c) => c.type === 'text') as
      | { type: 'text'; text: string }
      | undefined
    const optimized = textContent ? textContent.text : body.prompt

    return Response.json({ optimized })
  } catch (error) {
    // 7) 代理不可达时给出明确提示
    if (error instanceof APIConnectionError) {
      const hint = `Cannot connect to API endpoint at ${baseURL}. Check your provider settings and network.`
      return errorResponse(new ApiError(502, hint, 'API_CONNECTION_ERROR'))
    }
    return errorResponse(error)
  }
}
```

### 4.2 路由注册 —— `src/server/router.ts`

在路由分发处加入（与现有 `case` 平级）：

```ts
case 'optimize':
  return handleOptimizeApi(req)
```

顶部 import：

```ts
import { handleOptimizeApi } from './api/optimize.js'
```

> 本项目路由约定：URL 形如 `/api/<name>`，第一段 path 进入 `switch`。新项目可按自身路由框架等价注册 `POST /api/optimize`。

### 4.3 服务商配置读取 —— `readActiveProviderManagedEnv`

`src/server/services/providerRuntimeEnv.ts` 提供该函数，核心逻辑：

```ts
export function readActiveProviderManagedEnv(
  configDir: string,
): Record<string, string> | null {
  try {
    const raw = fs.readFileSync(path.join(configDir, 'cc-haha', 'providers.json'), 'utf-8')
    const index = normalizeProvidersIndex(JSON.parse(raw))
    if (!index?.activeId) return null
    const provider = index.providers.find((entry) => entry.id === index.activeId)
    if (!provider) return null
    return buildProviderManagedEnv(provider)
  } catch {
    return null
  }
}
```

其中 `buildProviderManagedEnv(provider)` 输出（简化）：

```ts
{
  ANTHROPIC_BASE_URL: provider.baseUrl,            // 服务商地址
  ANTHROPIC_API_KEY: provider.apiKey,              // 或 ANTHROPIC_AUTH_TOKEN
  ANTHROPIC_MODEL: provider.models.main,           // 主模型
  ANTHROPIC_DEFAULT_HAIKU_MODEL: provider.models.haiku, // 优化接口用 haiku 档
  // ...
}
```

新项目如果已有自己的服务商存储，只需保证 `handleOptimizeApi` 里能拿到 `baseURL / apiKey / authToken / model` 四项即可，不必照搬本文件。

---

## 5. 前端实现

### 5.1 API 封装 —— `desktop/src/api/sessions.ts`

```ts
optimizePrompt(prompt: string) {
  return api.post<{ optimized: string }>('/api/optimize', { prompt }, {
    timeout: 30_000,
  })
},
```

### 5.2 ⚡ 按钮 + 点击逻辑 —— `desktop/src/components/chat/ChatInput.tsx`

**状态**（组件顶部，与 `input` 同级）：

```ts
const [optimizeLoading, setOptimizeLoading] = useState(false)
```

**点击逻辑**（放在发送按钮的 `handleSubmit` 附近）：

```ts
const handleOptimize = useCallback(async () => {
  const text = input.trim()
  if (!text || optimizeLoading) return

  setOptimizeLoading(true)
  try {
    const result = await sessionsApi.optimizePrompt(text)
    if (result.optimized && result.optimized !== text) {
      setComposerInput(result.optimized)   // 优化结果回填输入框
    }
  } catch (error) {
    useUIStore.getState().addToast({
      type: 'error',
      message: error instanceof Error ? error.message : t('chat.optimizePromptFailed'),
    })
  } finally {
    setOptimizeLoading(false)
  }
}, [input, optimizeLoading, setComposerInput, t])
```

**按钮 JSX**（放在发送按钮之前，同一工具栏行）：

```tsx
{!isMemberSession && (
  <button
    onClick={handleOptimize}
    disabled={!input.trim() || optimizeLoading}
    aria-label={t('chat.optimizePrompt')}
    title={t('chat.optimizePrompt')}
    className={`flex shrink-0 items-center justify-center rounded-lg text-[var(--color-text-secondary)] transition-all
      hover:bg-[var(--color-surface-hover)] hover:text-[var(--color-text-primary)] disabled:opacity-30 ${
        isMobileComposer ? 'h-11 w-11 rounded-xl' : 'h-8 w-8'
      }`}
  >
    <span className={`material-symbols-outlined text-[18px] ${optimizeLoading ? 'animate-spin' : ''}`}>
      {optimizeLoading ? 'progress_activity' : 'auto_awesome'}
    </span>
  </button>
)}
```

> 图标说明：`auto_awesome`（Material Symbols 闪光图标，视觉上是 ⚡/✨ 效果）；加载中换成 `progress_activity` 并旋转。

### 5.3 i18n 文案 —— `desktop/src/i18n/locales/*.ts`

在各语言包的 `chat` 段加入（⚠️ 原项目当时漏了这步，新项目应补上）：

```ts
// en.ts
'chat.optimizePrompt': 'Optimize prompt',
'chat.optimizePromptFailed': 'Failed to optimize prompt',

// zh.ts
'chat.optimizePrompt': '优化提示词',
'chat.optimizePromptFailed': '提示词优化失败',
```

---

## 6. 数据结构：providers.json 示例

`<configDir>/cc-haha/providers.json`（服务商管理模块写入）：

```json
{
  "schemaVersion": 1,
  "activeId": "my-provider",
  "providers": [
    {
      "id": "my-provider",
      "presetId": "custom",
      "name": "我的服务商",
      "baseUrl": "https://api.example.com",
      "apiKey": "sk-xxx",
      "authStrategy": "api_key",
      "apiFormat": "anthropic",
      "models": {
        "main": "deepseek-v4",
        "haiku": "deepseek-v4",
        "sonnet": "deepseek-v4",
        "opus": "deepseek-v4"
      }
    }
  ]
}
```

## 7. 验证清单

| # | 操作 | 预期 |
|---|---|---|
| 1 | 配置并激活一个服务商（providers.json 有 activeId） | — |
| 2 | 输入框输入模糊提示词（如"写个登录功能"） | ⚡ 按钮可点击 |
| 3 | 点击 ⚡ | 按钮旋转进入加载态 |
| 4 | 等待返回 | 输入框被替换为优化后的完整提示词 |
| 5 | 未配置服务商时点击 | 弹出「未配置服务商」错误，输入框不变 |
| 6 | 断网 / 服务商地址错误时点击 | 弹出「Cannot connect to API endpoint at …」错误 |

## 8. 常见问题

| 问题 | 处理 |
|---|---|
| 返回内容带 thinking 前缀 | 已按 `type === 'text'` 过滤 text block，跳过思考内容 |
| 优化结果与原文相同 | 前端判断 `optimized !== text` 才回填，避免无意义替换 |
| 慢模型下前端先超时 | 前端 30s / 后端 600s；可调 `API_TIMEOUT_MS` 或前端 timeout |
| 提示词为空 | 按钮 `disabled={!input.trim()}`，后端也校验空 prompt 返回 400 |
