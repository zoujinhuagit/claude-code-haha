# 跨盘符目录浏览功能 — 改动文档与升级指南

## 功能概述

解决 Windows 桌面端新建会话时,目录选择器只能选 C 盘、无法导航到其他盘符(D:、E: 等)的问题。
同时保证:选定项目目录后才开放文件读写权限,其余目录仍然受安全白名单保护。

---

## 改动文件清单

| # | 文件路径 | 改动性质 |
|---|---------|---------|
| 1 | `src/server/api/filesystem.ts` | 后端浏览逻辑 + 安全分级 |
| 2 | `desktop/src/components/shared/DirectoryPicker.tsx` | 前端 UI 三处增强 |
| 3 | `desktop/src/i18n/locales/zh.ts` | 中文翻译新增 2 条 |
| 4 | `desktop/src/i18n/locales/en.ts` | 英文翻译新增 2 条 |

---

## 文件 1:`src/server/api/filesystem.ts`

### 改动点 A — 新增导入(第 15 行之后)

**在以下导入之后:**
```typescript
import { isWithinRegisteredFilesystemRoot } from '../services/filesystemAccessRoots.js'
```

**新增这一行:**
```typescript
import { containsVulnerableUncPath } from '../../utils/shell/readOnlyCommandValidation.js'
```

### 改动点 B — 新增函数 `getWindowsDriveEntries`(第 54 行 `isVcsMetadataDirectoryName` 函数之后)

```typescript
/** List available drive letters on Windows (C-Z that exist on disk). */
function getWindowsDriveEntries(resolvedPath: string): FilesystemEntry[] {
  const driveMatch = resolvedPath.match(/^([A-Za-z]):[\\]?$/)
  if (!driveMatch) return []
  const currentDrive = driveMatch[0].toLowerCase()

  const drives: FilesystemEntry[] = []
  for (let letter = 'C'.charCodeAt(0); letter <= 'Z'.charCodeAt(0); letter++) {
    const driveLetter = String.fromCharCode(letter)
    const drivePath = `${driveLetter}:\\`
    if (drivePath.toLowerCase() === currentDrive) continue
    try {
      if (fs.statSync(drivePath).isDirectory()) {
        drives.push({ name: drivePath, path: drivePath, isDirectory: true, relativePath: drivePath })
      }
    } catch { /* skip unmapped drives */ }
  }
  drives.sort((a, b) => a.name.localeCompare(b.name))
  return drives
}
```

**⚠️ 关键注意:**
- 正则表达式 `[\\]` 表示匹配一个反斜杠,不要写成 `[\\\\]`
- TypeScript 字符串中 `\\` 表示一个字面量 `\`

### 改动点 C — `handleBrowse` 安全分级(第 152 行开始)

**旧代码:**
```typescript
async function handleBrowse(url: URL): Promise<Response> {
  const targetPath = url.searchParams.get('path') || os.homedir() || '/'
  const resolvedPath = path.resolve(normalizeDriveRootPathForPlatform(targetPath))

  if (!isAllowedFilesystemPath(resolvedPath)) {
    return json({ error: 'Access denied: path outside allowed directory' }, 403)
  }

  const searchQuery = url.searchParams.get('search') || ''
  const includeFiles = url.searchParams.get('includeFiles') === 'true'
```

**新代码:**
```typescript
async function handleBrowse(url: URL): Promise<Response> {
  const targetPath = url.searchParams.get('path') || os.homedir() || '/'
  const resolvedPath = path.resolve(normalizeDriveRootPathForPlatform(targetPath))

  const searchQuery = url.searchParams.get('search') || ''
  const includeFiles = url.searchParams.get('includeFiles') === 'true'

  const isFileSearch = includeFiles || !!searchQuery
  if (isFileSearch) {
    if (!isAllowedFilesystemPath(resolvedPath)) {
      return json({ error: 'File search is limited to project directories' }, 403)
    }
  } else {
    if (containsVulnerableUncPath(resolvedPath)) {
      return json({ error: 'UNC paths are not supported' }, 403)
    }
  }
```

**⚠️ 关键注意:**
- **必须先定义** `searchQuery` 和 `includeFiles`,**再使用**它们
- 变量顺序错误会导致 `ReferenceError`

**逻辑说明:**
- 纯目录浏览(无搜索、无文件列表)→ 仅拦截 UNC 网络路径,所有本地盘符自由导航
- 文件搜索 / 文件列表 → 继续使用 `isAllowedFilesystemPath` 白名单

### 改动点 D — 注入盘符条目(第 180 行 `entries_list` 排序后、`return json(...)` 前)

**在以下代码之后:**
```typescript
      .sort((a, b) => {
        if (a.isDirectory !== b.isDirectory) return a.isDirectory ? -1 : 1
        return a.name.localeCompare(b.name)
      })

    return json({
```

**插入:**
```typescript
      .sort((a, b) => {
        if (a.isDirectory !== b.isDirectory) return a.isDirectory ? -1 : 1
        return a.name.localeCompare(b.name)
      })

    const siblingDrives = getWindowsDriveEntries(resolvedPath)
    if (siblingDrives.length > 0) {
      entries_list.unshift(...siblingDrives)
    }

    return json({
```

---

## 文件 2:`desktop/src/components/shared/DirectoryPicker.tsx`

### 改动点 A — 新增状态 `pathInput`(第 46 行附近)

**在以下代码之后:**
```typescript
  const [browseParent, setBrowseParent] = useState('')
```

**新增:**
```typescript
  const [pathInput, setPathInput] = useState('')
```

### 改动点 B — `loadBrowseDir` 中清空输入(第 122 行附近)

**在以下代码之后:**
```typescript
      setBrowseEntries(result.entries)
```

**新增:**
```typescript
      setPathInput('')
```

### 改动点 C — 新增 `handlePathSubmit` 函数(第 125 行附近)

**在 `loadBrowseDir` 函数结束后、`handleSelect` 函数之前:**

```typescript
  }

  const handlePathSubmit = () => {
    const trimmed = pathInput.trim()
    if (!trimmed) return
    loadBrowseDir(trimmed)
  }

  const handleSelect = (path: string) => {
```

### 改动点 D — Windows 面包屑路径(第 247-256 行)

**旧代码:**
```tsx
        {browsePath.split('/').filter(Boolean).map((seg, i, arr) => (
          <span key={i} className="flex items-center gap-1">
            <span className="text-[10px] text-[var(--color-text-tertiary)]">/</span>
            <button
              onClick={() => loadBrowseDir('/' + arr.slice(0, i + 1).join('/'))}
              className="text-[10px] text-[var(--color-text-accent)] hover:underline"
            >{seg}</button>
          </span>
        ))}
```

**新代码:**
```tsx
        {(() => {
          // Windows path detection: C:\...
          const isWindowsPath = browsePath.match(/^[A-Za-z]:[\\/]/)
          if (isWindowsPath) {
            const parts = browsePath.split('\\').filter(Boolean)
            const driveRoot = parts[0] // e.g., "C:"
            return parts.slice(1).map((seg, i, arr) => (
              <span key={i} className="flex items-center gap-1">
                <span className="text-[10px] text-[var(--color-text-tertiary)]">\\</span>
                <button
                  onClick={() => loadBrowseDir(driveRoot + '\\' + arr.slice(0, i + 1).join('\\'))}
                  className="text-[10px] text-[var(--color-text-accent)] hover:underline"
                >{seg}</button>
              </span>
            ))
          }
          // POSIX path: /home/user/...
          return browsePath.split('/').filter(Boolean).map((seg, i, arr) => (
            <span key={i} className="flex items-center gap-1">
              <span className="text-[10px] text-[var(--color-text-tertiary)]">/</span>
              <button
                onClick={() => loadBrowseDir('/' + arr.slice(0, i + 1).join('/'))}
                className="text-[10px] text-[var(--color-text-accent)] hover:underline"
              >{seg}</button>
            </span>
          ))
        })()}
```

**⚠️ 关键注意:**
- 正则表达式 `/^[A-Za-z]:[\\/]/` 中 `[\\/]` 表示匹配 `\` 或 `/`
- 不要写成 `[\\\\/]`,会导致匹配失败

### 改动点 E — 手动路径输入框(第 257 行之后,在面包屑 `</div>` 后)

**在面包屑区域的 `</div>` 之后、目录列表的 `<div>` 之前,新增:**

```tsx
      </div>

      <div className="flex items-center gap-1 border-b border-[var(--color-border)] px-3 py-1.5">
        <span className="material-symbols-outlined text-[14px] text-[var(--color-text-tertiary)]">edit</span>
        <input
          type="text"
          value={pathInput}
          onChange={(e) => setPathInput(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === 'Enter') handlePathSubmit()
          }}
          placeholder={browsePath || t('dirPicker.typePath')}
          className="flex-1 bg-transparent text-[11px] font-[var(--font-mono)] text-[var(--color-text-primary)] outline-none placeholder:text-[var(--color-text-tertiary)]"
        />
        <button
          onClick={handlePathSubmit}
          className="rounded px-2 py-0.5 text-[10px] font-semibold text-[var(--color-brand)] transition-colors hover:bg-[var(--color-primary-fixed)]"
        >
          Go
        </button>
      </div>

      <div className={`${isMobileBrowser ? '' : 'max-h-[240px]'} overflow-y-auto`}>
```

### 改动点 F — 盘符根目录 ".." 提示(第 264-269 行)

**旧代码:**
```tsx
            {browseParent && browseParent !== browsePath && (
              <button onClick={() => loadBrowseDir(browseParent)} className="flex w-full items-center gap-2 px-3 py-2 text-left hover:bg-[var(--color-surface-hover)]">
                <span className="material-symbols-outlined text-[16px] text-[var(--color-text-tertiary)]">arrow_upward</span>
                <span className="text-xs text-[var(--color-text-secondary)]">..</span>
              </button>
            )}
```

**新代码:**
```tsx
            {(browseParent && browseParent !== browsePath) ? (
              <button onClick={() => loadBrowseDir(browseParent)} className="flex w-full items-center gap-2 px-3 py-2 text-left hover:bg-[var(--color-surface-hover)]">
                <span className="material-symbols-outlined text-[16px] text-[var(--color-text-tertiary)]">arrow_upward</span>
                <span className="text-xs text-[var(--color-text-secondary)]">..</span>
              </button>
            ) : browsePath.match(/^[A-Za-z]:[\\/]?$"/) ? (
              <div className="px-3 py-1.5 text-[10px] font-bold uppercase tracking-widest text-[var(--color-outline)]">{t('dirPicker.availableDrives')}</div>
            ) : null}
```

**⚠️ 关键注意:**
- 正则表达式 `/^[A-Za-z]:[\\/]?$"/` 匹配盘符根目录如 `C:` 或 `C:\`
- 使用三元表达式替代简单的 `&&` 条件渲染

---

## 文件 3 & 4:i18n 翻译文件

在 `desktop/src/i18n/locales/zh.ts` 和 `desktop/src/i18n/locales/en.ts` 中,`dirPicker` 段末尾各新增 2 条:

### 中文 (`zh.ts` 第 1629 行之后)

**搜索定位:** `'dirPicker.noSubdirs': '无子目录',`

**在它下一行追加:**
```typescript
  'dirPicker.noSubdirs': '无子目录',
  'dirPicker.availableDrives': '切换盘符:',
  'dirPicker.typePath': '输入或粘贴路径...',
```

### 英文 (`en.ts` 第 1627 行之后)

**搜索定位:** `'dirPicker.noSubdirs': 'No subdirectories',`

**在它下一行追加:**
```typescript
  'dirPicker.noSubdirs': 'No subdirectories',
  'dirPicker.availableDrives': 'Available drives:',
  'dirPicker.typePath': 'Type or paste a path...',
```

> ⚠️ 注意:TranslationKey 类型定义由 `en.ts` 导出,新增 key 必须同时出现在 en.ts 和 zh.ts 中,否则编译报错。

---

## 升级替换指南

### 场景 1:完全覆盖升级(简单粗暴)

直接复制上述 4 个文件覆盖即可。缺点是如果有其他自定义改动会丢失。

### 场景 2:逐文件合并(推荐)

按以下优先级顺序执行合并:

#### 1️⃣ `desktop/src/i18n/locales/en.ts` + `zh.ts`(最容易)

在 `'dirPicker.noSubdirs'` 后面加两行:
```
'dirPicker.availableDrives': '...',
'dirPicker.typePath': '...',
```
搜索 `dirPicker.noSubdirs` 定位,在它下一行追加即可。

#### 2️⃣ `src/server/api/filesystem.ts`(后端核心)

按位置逐段替换:

| 步骤 | 搜索定位关键字 | 操作 |
|------|--------------|------|
| 1 | `import { isWithinRegisteredFilesystemRoot }` | 在这行上方添加 `containsVulnerableUncPath` 导入 |
| 2 | `function isVcsMetadataDirectoryName` | 在函数结束 `}` 后添加 `getWindowsDriveEntries` 函数 |
| 3 | `async function handleBrowse` | **注意变量顺序**:先定义 `searchQuery` 和 `includeFiles`,再使用 `isFileSearch` 检查 |
| 4 | `entries_list` 排序后 | 在 `return json({` 前插入 `siblingDrives` 注入逻辑 |

**⚠️ 易错点:**
- `handleBrowse` 中变量定义顺序必须先于使用
- 正则表达式 `[\\]` 不要写成 `[\\\\]`

#### 3️⃣ `desktop/src/components/shared/DirectoryPicker.tsx`(前端 UI)

按位置逐段替换:

| 步骤 | 搜索定位关键字 | 操作 |
|------|--------------|------|
| 1 | `const [browseParent, setBrowseParent]` | 在它下一行加 `const [pathInput, setPathInput] = useState('')` |
| 2 | `setBrowseEntries(result.entries)` | 在它下一行加 `setPathInput('')` |
| 3 | `const handleSelect = (path` | 在它上方加 `handlePathSubmit` 函数 |
| 4 | `{browsePath.split('/')`（面包屑区域） | 整体替换为 IIFE 双模式面包屑 + 输入框 |
| 5 | `browseParent && browseParent !== browsePath` | 替换为三元 + 盘符提示的新逻辑 |

**⚠️ 易错点:**
- 面包屑正则 `/^[A-Za-z]:[\\/]/` 不要写成 `/^[A-Za-z]:[\\\\/]/`
- 盘符检测正则 `/^[A-Za-z]:[\\/]?$"/` 不要写成 `/^[A-Za-z]:[\\\\/]?$"/`

### 场景 3:增量 patch(Git 友好)

```bash
# 查看当前改动相对原始版本的 diff
git diff HEAD -- src/server/api/filesystem.ts > cross-drive-backend.patch
git diff HEAD -- desktop/src/components/shared/DirectoryPicker.tsx > cross-drive-frontend.patch
git diff HEAD -- desktop/src/i18n/locales/zh.ts desktop/src/i18n/locales/en.ts > cross-drive-i18n.patch

# 在新版本中应用
git apply cross-drive-backend.patch
git apply cross-drive-frontend.patch
git apply cross-drive-i18n.patch
```

### 容易合并冲突的位置

| 位置 | 风险 | 原因 |
|------|------|------|
| `handleBrowse` 安全检查段 | 中 | 原 `isAllowedFilesystemPath` 调用被替换,且变量顺序调整 |
| `DirectoryPicker` 面包屑区域 | 高 | 原 10 行被替换为约 40 行自定义 JSX |
| i18n key 列表末尾 | 低 | 只是追加两行 |

---

## 已知问题与修复记录

### 问题 1:变量引用顺序错误

**症状:** `ReferenceError: Cannot access 'includeFiles' before initialization`

**原因:** `isFileSearch` 在 `searchQuery` 和 `includeFiles` 定义之前被使用

**修复:** 调整变量定义顺序
```typescript
// ✅ 正确顺序
const searchQuery = url.searchParams.get('search') || ''
const includeFiles = url.searchParams.get('includeFiles') === 'true'
const isFileSearch = includeFiles || !!searchQuery
```

### 问题 2:正则表达式过度转义

**症状:** 盘符无法匹配,`getWindowsDriveEntries` 返回空数组

**原因:** 
- 后端:`[\\\\]` 尝试匹配两个反斜杠,应该用 `[\\]`
- 前端:`[\\\\/]` 应该用 `[\\/]`

**修复:**
```typescript
// ✅ 后端正确写法
const driveMatch = resolvedPath.match(/^([A-Za-z]):[\\]?$/)

// ✅ 前端正确写法
const isWindowsPath = browsePath.match(/^[A-Za-z]:[\\/]/)
browsePath.match(/^[A-Za-z]:[\\/]?$"/)
```

**技术说明:**
- TypeScript 字符串中 `\\` 表示一个字面量 `\`
- 正则表达式字符类 `[\\]` 匹配一个反斜杠
- 过度转义 `[\\\\]` 会匹配两个反斜杠,导致失败

---

## 验证清单

升级完成后执行以下检查:

```bash
# 1. 安装依赖(如果未安装)
cd desktop && bun install

# 2. TypeScript 编译检查
cd desktop && bun run lint

# 3. 如果桌面端处于开发模式,热重载会自动生效
cd desktop && bun run dev

# 4. 手动验证:
#    - 打开目录选择器 → 浏览到 C:\ 根目录 → 应看到 D: E: 等盘符条目
#    - 在输入框中输入 D:\  按 Go → 应跳转到 D 盘
#    - 在输入框中直接粘贴完整路径(如 D:\MyProject)按 Enter → 应跳转
#    - 选定目录创建会话后 → 文件搜索/预览应正常工作
#    - 面包屑导航应正确显示 Windows 路径分隔符 \n```

### 验证检查表

- [ ] C 盘根目录显示其他可用盘符(D:、E: 等)
- [ ] 点击盘符条目能跳转到对应盘
- [ ] 手动输入路径能正常导航
- [ ] 回车键和 Go 按钮都能提交
- [ ] Windows 面包屑使用 `\` 分隔符
- [ ] POSIX 面包屑使用 `/` 分隔符(跨平台兼容)
- [ ] 文件搜索仍受白名单保护
- [ ] UNC 网络路径被拦截
- [ ] TypeScript 编译无错误

---

## 技术要点总结

### 安全设计

1. **分级权限控制**
   - 目录浏览:仅拦截 UNC 路径,允许本地盘符自由导航
   - 文件搜索:继续使用 `isAllowedFilesystemPath` 白名单

2. **UNC 路径防护**
   - 使用 `containsVulnerableUncPath` 检测网络路径
   - 防止凭据泄露到外部服务器

### 跨平台兼容

1. **Windows 路径处理**
   - 盘符根目录:`C:` 或 `C:\`
   - 路径分隔符:`\`
   - 正则:`/^[A-Za-z]:[\\/]/`

2. **POSIX 路径处理**
   - 根目录:`/`
   - 路径分隔符:`/`
   - 原有逻辑保持不变

### 用户体验

1. **多种导航方式**
   - 点击盘符条目
   - 手动输入路径
   - 面包屑导航
   - 传统 `..` 返回上级

2. **即时反馈**
   - 盘符根目录显示"切换盘符:"提示
   - 输入框占位符显示当前路径
   - 加载状态提示

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-02-06 | 初始版本,实现跨盘符浏览功能 |
| v1.1 | 2026-02-06 | 修复变量引用顺序错误 |
| v1.2 | 2026-02-06 | 修复正则表达式过度转义问题 |
