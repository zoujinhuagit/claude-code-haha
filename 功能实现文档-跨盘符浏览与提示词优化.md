# 功能实现文档：跨盘符目录浏览 + ⚡ 提示词优化

> 本文是这两个功能**已落地状态**的合并记录，对照实际代码整理，可直接用于交接与排障。
> 原始设计稿 `跨盘符目录浏览功能.md`、`提示词功能.md` 保留作历史参考，**以本文为准**。
>
> 最后更新：2026-09-19

---

## 0. 总览

| # | 功能 | 入口 | 状态 |
|---|---|---|---|
| 1 | 跨盘符目录浏览 | 新建会话 → 目录选择器 → 浏览模式 | 已完成 |
| 2 | ⚡ 提示词优化 | 会话输入框工具栏，圆发送按钮左侧 | 已完成 |

### 改动文件一览

**功能 1**

| 文件 | 性质 |
|---|---|
| `src/server/api/filesystem.ts` | 后端：浏览安全分级 + 盘符枚举 |
| `desktop/src/components/composite/DirectoryPicker.tsx` | 前端：路径输入框、双模式面包屑、盘符分组 |
| `desktop/src/i18n/locales/{en,zh,zh-TW,jp,kr}.ts` | 新增 3 个 `dirPicker.*` key |
| `src/server/__tests__/filesystem.test.ts` | 1 条断言随安全模型变更 |
| `desktop/src/components/composite/DirectoryPicker.test.tsx` | 新增 4 个用例 |

**功能 2**

| 文件 | 性质 |
|---|---|
| `src/server/services/providerCompletion.ts` | **新增**：服务商解析 + 一次性文本生成 |
| `src/server/api/optimize.ts` | **新增**：`POST /api/optimize` |
| `src/server/router.ts` | 注册 `case 'optimize'` |
| `src/server/services/titleService.ts` | 改为复用 `providerCompletion`（净减约 230 行） |
| `desktop/src/api/sessions.ts` | 新增 `sessionsApi.optimizePrompt` |
| `desktop/src/components/chat/ChatInput.tsx` | ⚡ 按钮 + 点击逻辑 |
| `desktop/src/i18n/locales/{en,zh,zh-TW,jp,kr}.ts` | 新增 2 个 `chat.*` key |
| `src/server/__tests__/optimize-api.test.ts` | **新增**：11 个用例 |
| `desktop/src/components/chat/ChatInput.test.tsx` | 新增 4 个用例 + mock 补 `optimizePrompt` |

---

## 1. 跨盘符目录浏览

### 1.1 要解决的问题

Windows 上新建会话时，目录选择器只能进 C 盘。原因是 `handleBrowse` 对**所有**浏览请求都套 `isAllowedFilesystemPath` 白名单，而白名单只包含 `os.homedir()`、`/tmp` 和已注册的项目根目录；`D:\`、`E:\` 永远不会被注册，所以导航到盘符根目录就被 403 挡死。

### 1.2 安全模型（分层）

放开的是**目录浏览**，不是文件访问。这一层区分是本次改动的核心：

| 请求形态 | 校验方式 | 理由 |
|---|---|---|
| 纯目录浏览（无 `search`、无 `includeFiles`） | **只拦 UNC 网络路径** | 这是用户走到别的盘去挑项目文件夹的路径。列 `D:\` 不泄露用户看不见的东西 |
| 文件搜索 / 文件列表（`search` 或 `includeFiles=true`） | 保留 `isAllowedFilesystemPath` 白名单 | 这两个模式会把**文件名和内容**喂给模型 |
| `GET /api/filesystem/file`（读文件字节） | 保留白名单（未改动） | 真正的读取边界 |

UNC 拦截用 `containsVulnerableUncPath`（`src/utils/shell/readOnlyCommandValidation.ts:1562`），它在非 Windows 上直接返回 `false`，Windows 上识别 `\\host`、`//host`、`DavWWWRoot`、`@SSL@`、`\\<ipv4>` 等形态，防的是凭据被送到外部服务器。

> ⚠️ **已知残留风险**：映射网络驱动器（`Z:` → 共享目录）在路径形态上与本地盘符无法区分，`containsVulnerableUncPath` 拦不住。
>
> ⚠️ **H5 远程**：`/api/filesystem/browse` 对已配对的远程 H5 客户端是可达的（`router.ts` 的 `remoteBrowser` 特权收敛只覆盖 `providers` / `settings`）。本次按决策**不加调用方限制**，即远程客户端也可以枚举全盘目录。若要收紧，需要把 `ApiRequestContext` 透传进 `handleFilesystemRoute`，仅在 `remoteBrowser !== true` 时放开。

### 1.3 后端实现 —— `src/server/api/filesystem.ts`

**a. 盘符枚举**

```ts
/**
 * 其他盘符，仅 Windows。
 *
 * `statSync` 就是存在性判断：未映射的盘符会抛错，正是「不是盘」的答案。
 * 当前路径不是盘符根时返回空，避免盘符列表出现在每个目录里 ——
 * 调用方只在用户走到 `C:\` 时才问
 */
function getWindowsDriveEntries(currentDirectory: string): FilesystemEntry[] {
  const driveMatch = currentDirectory.match(/^([A-Za-z]):\\?$/)
  if (!driveMatch) return []
  const currentDrive = `${driveMatch[1]}:\\`.toLowerCase()

  const drives: FilesystemEntry[] = []
  for (let letter = 'C'.charCodeAt(0); letter <= 'Z'.charCodeAt(0); letter++) {
    const drivePath = `${String.fromCharCode(letter)}:\\`
    if (drivePath.toLowerCase() === currentDrive) continue
    try {
      if (fs.statSync(drivePath).isDirectory()) {
        drives.push({ name: drivePath, path: drivePath, isDirectory: true, relativePath: drivePath })
      }
    } catch {
      // 未映射的盘符 stat 会抛错 —— 这就是「不是盘」的答案
    }
  }
  return drives.sort((a, b) => a.name.localeCompare(b.name))
}
```

要点：
- 用**捕获组** `driveMatch[1]` 拼当前盘，而不是 `driveMatch[0]`——后者在匹配 `C:\` 时会带上反斜杠，比较不一致。
- `statSync` 就是存在性判断，未映射的盘符会抛错，无需额外探测。
- 仅当当前目录是盘符根时才返回结果，所以普通目录里不会冒出盘符列表。
- 正则写成 `/^([A-Za-z]):\\?$/`（TS 字符串里 `\\` 是一个字面反斜杠），**不要**写成 `[\\\\]`——那会去匹配两个反斜杠。

**b. 分级校验**

```ts
async function handleBrowse(url: URL): Promise<Response> {
  const targetPath = url.searchParams.get('path') || os.homedir() || '/'
  const resolvedPath = path.resolve(normalizeDriveRootPathForPlatform(targetPath))

  // 必须先读这两个参数再校验：用哪种校验取决于模式，
  // 而 const 在声明前被读取是 TDZ 错误，不是 lint 提示
  const searchQuery = url.searchParams.get('search') || ''
  const includeFiles = url.searchParams.get('includeFiles') === 'true'
  const maxResults = Math.min(parseInt(url.searchParams.get('maxResults') || '200', 10), 200)

  const isFileSearch = includeFiles || !!searchQuery
  const findDenial = (candidate: string): Response | null => {
    if (isFileSearch) {
      return isAllowedFilesystemPath(candidate)
        ? null
        : json({ error: 'File search is limited to project directories' }, 403)
    }
    return containsVulnerableUncPath(candidate)
      ? json({ error: 'UNC paths are not supported' }, 403)
      : null
  }

  const deniedResolved = findDenial(resolvedPath)
  if (deniedResolved) return deniedResolved

  const canonicalPath = await canonicalizeExistingFilesystemPath(resolvedPath)
  if (!canonicalPath) {
    return json({ error: 'Cannot read directory: path not found', path: resolvedPath }, 404)
  }
  // canonical 路径也要再查一次：白名单根目录里的符号链接可以解析到别处，
  // 而真正被读取的是解析后的目标
  const deniedCanonical = findDenial(canonicalPath)
  if (deniedCanonical) return deniedCanonical
  ...
```

**c. 盘符注入**（在 `entries_list` 排序之后、`return json({` 之前）

```ts
// 盘符排在列表最前：`..` 在盘符根没有去处，
// 这是横向走到 `D:\` 的唯一入口
const siblingDrives = getWindowsDriveEntries(canonicalPath)
if (siblingDrives.length > 0) entries_list.unshift(...siblingDrives)
```

用 `canonicalPath` 而不是 `resolvedPath`，否则判断不出「当前在哪个盘」。

### 1.4 前端实现 —— `desktop/src/components/composite/DirectoryPicker.tsx`

> 路径是 **`composite/`**，原始设计稿写的 `shared/` 是错的。

**a. 状态与导航**

```ts
const [pathInput, setPathInput] = useState('')

const loadBrowseDir = async (path?: string) => {
  setLoading(true)
  let loaded = false
  try {
    const result = await filesystemApi.browse(path)
    setBrowsePath(result.currentPath)
    setBrowseParent(result.parentPath)
    setBrowseEntries(result.entries)
    setPathInput('')
    loaded = true
  } catch { /* API not available */ }
  setLoading(false)
  return loaded      // ← 返回值供输入框判断成功与否
}

// 手输路径是唯一可能输错的一种导航，所以要答话：
// 面板里其他操作都是服务端已经担保过的条目。
// 没有这个 toast，输错路径看起来就是控件坏了
const handlePathSubmit = async () => {
  const trimmed = pathInput.trim()
  if (!trimmed) return
  if (await loadBrowseDir(trimmed)) return
  useUIStore.getState().addToast({ type: 'error', message: t('dirPicker.pathNotFound') })
}
```

**b. 双模式面包屑**

`browsePath` 是后端 canonicalize 后的结果，可能是 `D:\work\app` 也可能是 `/home/u/app`。只按 `/` 切会把 Windows 路径变成一整段，所以分隔符、根标签、拼接规则都从路径自身形态推导（模块顶部另外定义了两个常量：`WINDOWS_DRIVE_ROOT_PATTERN = /^[A-Za-z]:[\\/]?$/` 判断盘符根，`WINDOWS_ABSOLUTE_PATTERN = /^([A-Za-z]):[\\/]/` 判断绝对路径）：

```ts
const breadcrumb = (() => {
  const windowsMatch = browsePath.match(WINDOWS_ABSOLUTE_PATTERN)
  if (windowsMatch) {
    const root = `${windowsMatch[1]}:\\`
    return {
      root,
      separator: '\\',
      segments: browsePath.slice(root.length).split('\\').filter(Boolean),
      join: (parts: string[]) => root + parts.join('\\'),
    }
  }
  return {
    root: '/',
    separator: '/',
    segments: browsePath.split('/').filter(Boolean),
    join: (parts: string[]) => '/' + parts.join('/'),
  }
})()
```

两个正则的写法（**易错点**）：`/^([A-Za-z]):[\\/]/` 中 `[\\/]` 是「反斜杠或斜杠」，写成 `[\\\\/]` 会匹配失败。

**c. 盘符分组标题**

```ts
// 服务端只在「有盘符可加」时才注入，且没有单独标记，
// 所以只看「当前在盘符根」会在单盘机器上宣布一个空分组
const showDriveHeading = WINDOWS_DRIVE_ROOT_PATTERN.test(browsePath)
  && browseEntries.some((entry) => WINDOWS_DRIVE_ROOT_PATTERN.test(entry.path))
```

列表首行二选一：

```tsx
{browseParent && browseParent !== browsePath ? (
  <button onClick={() => loadBrowseDir(browseParent)}>…arrow_upward… ..</button>
) : showDriveHeading ? (
  <div className="…">{t('dirPicker.availableDrives')}</div>
) : null}
```

**d. 手动路径输入框**（面包屑下方）

Enter 和 Go 按钮都能提交；`aria-label` 用 `dirPicker.typePath`。

### 1.5 i18n（5 个语言包都要加）

| key | en | zh | zh-TW | jp | kr |
|---|---|---|---|---|---|
| `dirPicker.availableDrives` | `Available drives:` | `切换盘符：` | `切換磁碟：` | `ドライブ：` | `드라이브:` |
| `dirPicker.typePath` | `Type or paste a path...` | `输入或粘贴路径...` | `輸入或貼上路徑...` | `パスを入力または貼り付け...` | `경로 입력 또는 붙여넣기...` |
| `dirPicker.pathNotFound` | `That folder could not be opened` | `无法打开该文件夹` | `無法開啟該資料夾` | `そのフォルダーを開けませんでした` | `해당 폴더를 열 수 없습니다` |

> `en.ts` 导出 `type TranslationKey = keyof typeof en`，另外 4 个是 `Record<TranslationKey, string>`（穷尽类型）。**少一个语言包就 `tsc --noEmit` 报错。**

### 1.6 测试

- `src/server/__tests__/filesystem.test.ts`（8 个用例）：原「纯目录浏览符号链接逃逸 → 403」断言改为 **200**，并在注释里说明边界已转移到 `/api/filesystem/file`（同用例内该断言保留 403）。该用例在 `win32` 直接 return，**需要 macOS/Linux 才能实际跑到**。
- `desktop/src/components/composite/DirectoryPicker.test.tsx`（14 个用例，新增 4）：盘符根显示分组标题且不渲染死掉的 `..`；Windows 路径面包屑按 `\` 拆分；输入路径回车导航；输入非法路径弹 toast。

---

## 2. ⚡ 提示词优化

### 2.1 功能定义

| 项 | 说明 |
|---|---|
| 入口 | 会话输入框工具栏，圆发送按钮**左侧**，`auto_awesome` 图标 |
| 交互 | 有内容 → 点击 → Spinner + 禁用 → 回填改写结果，光标移到末尾 |
| 规则 | 保留原意图、补模糊点、消歧义、多部分请求结构化、**保持用户原语言**、只输出改写后的文本 |
| 失败 | 弹 toast，输入框内容不变 |

⚠️ **作用范围**：项目里有**两个独立 composer**——`ChatInput.tsx`（会话中）和 `EmptySession.tsx`（首页空会话）。当前**只加在 `ChatInput.tsx`**。

### 2.2 架构：为什么必须复用 `providerCompletion`

服务端「调活动服务商的小模型」这件事，原本只在 `titleService` 里实现过一次。本次把它抽成 `src/server/services/providerCompletion.ts`，两个调用方共用。

自己写 `new Anthropic({ baseURL, apiKey })` 会在下面这些场景必然失败：

| 服务商类型 | 现象 |
|---|---|
| OpenAI 格式预设（`apiFormat: 'openai_chat'` / `'openai_responses'`，如 opencode-go、DeepSeek 类中转） | `POST /v1/messages` 被网关拒绝（404/400），需走 `/chat/completions` 或 `/responses` |
| OpenAI 官方 OAuth | 需要 ChatGPT OAuth token + Codex 协议 → 401 |
| `authStrategy` 为 `dual_same_token` / `dual_dummy` 的网关 | 401（只发 `x-api-key` 不够） |
| 模型名 | 硬编码回退到 `claude-haiku-*`，用户的服务商没有这个模型 → 400 model not found |
| `client.beta.*` | 部分第三方网关不实现 beta 命名空间 → 404 |

### 2.3 `src/server/services/providerCompletion.ts`（新增）

对外两个函数 + 结果类型：

```ts
export type ResolvedCompletionProvider =
  | { kind: 'openai-official'; model: string }
  | {
      kind: 'anthropic'
      id: string
      baseUrl: string
      apiKey: string
      authStrategy: ProviderAuthStrategy
      usesLocalProxy: boolean
      model: string
    }

/** 不传 providerId = 用活动服务商；显式传 null = 调用方决定不生成，返回 null */
export async function resolveCompletionProvider(
  providerId?: string | null,
): Promise<ResolvedCompletionProvider | null>

export type CompletionFailure =
  | { kind: 'http'; status: number; detail?: string }       // 上游非 2xx
  | { kind: 'no-text'; status: number; detail?: string }    // 2xx 但没有 text block
  | { kind: 'exception'; detail?: string }                  // 抛异常

export type CompletionResult =
  | { ok: true; text: string }
  | { ok: false; failure: CompletionFailure }

export async function completeWithProvider(
  resolved: ResolvedCompletionProvider,
  request: {
    system: string
    userContent: string
    maxTokens: number
    sessionId?: string   // 透传给代理，网关据此路由
    timeoutMs?: number   // 默认 15_000
  },
): Promise<CompletionResult>
```

**三条执行路径**（由 `resolveCompletionProvider` 预先判定）：

1. **直连** `POST {baseUrl}/v1/messages`，请求头按 5 种 `authStrategy` 映射（`x-api-key` / `Bearer` / 双写 / `dummy`）。
2. **进程内代理** `POST http://127.0.0.1/proxy/providers/{id}/v1/messages` —— 当 `providerNeedsProxy(resolveProviderApiFormat(p), p.supportsNestedToolResultMedia)` 为真时。代理负责 OpenAI ↔ Anthropic 协议转换与预设专属上游请求头，超时由代理自己管。
3. **OpenAI 官方 OAuth** —— `ensureFreshTokens()` → `resolveOpenAICodexModel` → `anthropicToOpenaiResponses` → Codex 端点 → `openaiResponsesStreamToAnthropicResponse`。

**两条路径共用的重试策略**：第一发带 `thinking: { type: 'disabled' }`，遇 4xx 去掉 `thinking` 重试一次（比逐模型探测能力便宜）。

**关键实现细节**：

```ts
const DEFAULT_COMPLETION_TIMEOUT_MS = 15_000   // 与原 titleService 一致
const FAILURE_DETAIL_MAX_LENGTH = 300

/**
 * 先把 body 读成文本再解析：直接 .json() 会丢掉原始 body，
 * 遇到非 JSON 或 SSE 就只能报「无内容」
 */
async function readAnthropicText(response: Response): Promise<CompletionResult> {
  const raw = await response.text()
  let body: { content?: Array<{ type: string; text?: string }> }
  try {
    body = JSON.parse(raw) as typeof body
  } catch {
    return { ok: false, failure: { kind: 'no-text', status: response.status, detail: `unparsable body: ${truncate(raw)}` } }
  }

  const text = body.content?.find((block) => block.type === 'text')?.text
  if (!text?.trim()) {
    // 推理模型可以只回 thinking block，不报 block 类型就只会显示「空回答」
    const blockTypes = body.content?.map((block) => block.type).join(', ') || 'none'
    return { ok: false, failure: { kind: 'no-text', status: response.status, detail: `content blocks: ${blockTypes}` } }
  }
  return { ok: true, text }
}
```

### 2.4 `src/server/api/optimize.ts`（新增）

```ts
export async function handleOptimizeApi(req: Request, _url: URL, _segments: string[]): Promise<Response>
```

常量：`MAX_PROMPT_LENGTH = 8000`、`OPTIMIZE_MAX_OUTPUT_TOKENS = 4096`、`OPTIMIZE_TIMEOUT_MS = 60_000`。

用户消息把原提示词包起来，避免模型把提示词内容当指令：

```
Improve the following prompt. Make it clearer, more specific, and more detailed while preserving the original intent.

Output ONLY the improved prompt text — no explanations or prefixes.

<prompt>
{用户输入}
</prompt>
```

约定（与 `src/server/api/models.ts` 一致）：路由层不检查 method，handler 自己判；顶层 `try/catch` + `errorResponse`；全局 `Response.json`；`parseJsonBody` 把解析失败转 400。

### 2.5 错误码与排查

| 情况 | 状态码 | `error` |
|---|---|---|
| 非 POST | 405 | `METHOD_NOT_ALLOWED` |
| body 非 JSON / `prompt` 非字符串或为空 | 400 | `BAD_REQUEST` |
| `prompt.length > 8000` | 400 | `BAD_REQUEST` |
| 解析不到活动服务商 | 409 | `NO_ACTIVE_PROVIDER` |
| 上游返回非 2xx | 502 | `OPTIMIZE_UPSTREAM_ERROR` |
| 上游 2xx 但没有 text block / body 不是 JSON | 502 | `OPTIMIZE_EMPTY_RESPONSE` |
| 抛异常（网络、超时、DNS、OAuth 缺 token） | 502 | `OPTIMIZE_FAILED` |
| 成功 | 200 | `{ optimized: string }`（已 trim） |

> ⚠️ **不要把所有失败压成一个 `null`**。初版让 `completeWithProvider` 失败即返回 `null`，于是 401、400、超时、「模型只回了 thinking」全部变成同一句
> 「The provider did not return an optimized prompt」——用户被指去检查本来没问题的设置，排查直接卡死。
> 现在的做法：服务端 `console.error('[Optimize] provider call failed', { provider, failure })`，并把上游 status + body 摘要带进 message。

拿到 message 后的对照排查：

| message 里出现 | 结论 |
|---|---|
| `HTTP 401` / `invalid x-api-key` | 服务商密钥或 `authStrategy` 不对 |
| `HTTP 404` / `model not found` | `models.haiku` 指向的模型该服务商没有 |
| `HTTP 400` + `max_tokens` | 模型输出上限小于 4096，调小 `OPTIMIZE_MAX_OUTPUT_TOKENS` |
| `content blocks: thinking` | 活动服务商选到了推理模型，换非推理的 haiku 档 |
| `unparsable body: data:` | 网关强制返回 SSE，需要走代理路径或按 SSE 解析 |
| `No OpenAI OAuth token` | 用的是 OpenAI 官方服务商但没登录 |

### 2.6 路由注册 —— `src/server/router.ts`

`resource = segments[1]`，所以 `/api/optimize` 命中 `'optimize'`：

```ts
import { handleOptimizeApi } from './api/optimize.js'

case 'optimize':
  return handleOptimizeApi(req, url, segments)
```

> 有一条测试专门走 `handleApiRequest` 而不是直接调 handler —— 直接调 handler 在漏注册路由时依然会通过，而漏注册正是「按钮点了没反应」的典型成因。

### 2.7 前端 —— `desktop/src/api/sessions.ts`

```ts
optimizePrompt(prompt: string, sessionId?: string) {
  return api.post<{ optimized: string }>(
    '/api/optimize',
    { prompt, sessionId },
    { timeout: 90_000 },   // 必须大于后端的 60s
  )
},
```

### 2.8 前端 —— `desktop/src/components/chat/ChatInput.tsx`

```ts
const [optimizeLoading, setOptimizeLoading] = useState(false)

// 从实时文档读取，mention pill 会被序列化成模型该看到的路径。
// 结果是纯文本，所以也会把 pill 替换掉 —— 见下面的回填
const handleOptimize = useCallback(async () => {
  if (optimizeLoading) return
  const text = (composerRef.current?.getModelContent() ?? inputRef.current).trim()
  if (!text) return

  setOptimizeLoading(true)
  try {
    const { optimized } = await sessionsApi.optimizePrompt(text, activeTabId ?? undefined)
    if (!optimized || optimized === text) return

    setComposerInput(optimized, [])          // ← 必须传 []，见下文根因 3
    requestAnimationFrame(() => {
      composerRef.current?.focus()
      composerRef.current?.setSelectionOffsets(optimized.length)
    })
  } catch (error) {
    useUIStore.getState().addToast({
      type: 'error',
      message: error instanceof Error ? error.message : t('chat.optimizePromptFailed'),
    })
  } finally {
    setOptimizeLoading(false)
  }
}, [activeTabId, optimizeLoading, setComposerInput, t])
```

按钮插在圆发送 `<Button>` 之前：

```tsx
{!isMemberSession && (
  <IconButton
    icon="auto_awesome"
    label={t('chat.optimizePrompt')}
    onClick={() => { void handleOptimize() }}
    disabled={!input.trim() || optimizeLoading}
    loading={optimizeLoading}
    size={isMobileComposer ? '2xl' : 'md'}   // 44px / 32px，与相邻发送按钮同档
    tone="muted"
    className="shrink-0"
  />
)}
```

要点：
- 用项目已有的 `IconButton`（`desktop/src/components/ui/IconButton.tsx`）。它的 `loading` 自带 Spinner + `disabled` + `aria-busy`，**不要自己写旋转图标**。
- 加载态是必要的：改写要跑几秒，一个「点了没反应」的按钮会被当成坏了。
- `getModelContent()` 把 mention pill 序列化成路径，那才是该发给模型的内容；写入时 pill 变成纯文本是预期行为。

### 2.9 i18n（5 个语言包都要加）

| key | en | zh | zh-TW | jp | kr |
|---|---|---|---|---|---|
| `chat.optimizePrompt` | `Optimize prompt` | `优化提示词` | `最佳化提示詞` | `プロンプトを最適化` | `프롬프트 최적화` |
| `chat.optimizePromptFailed` | `Failed to optimize prompt` | `提示词优化失败` | `提示詞最佳化失敗` | `プロンプトの最適化に失敗しました` | `프롬프트 최적화 실패` |

### 2.10 测试

**后端 `src/server/__tests__/optimize-api.test.ts`（11 个用例）**

打桩沿用 `title-service.test.ts` 的方式：`CLAUDE_CONFIG_DIR` 指向临时目录 + `ProviderService().addProvider({...})` + `activateProvider(id)`；用 `Bun.serve({ hostname: '127.0.0.1', port: 0 })` 返回 `Response.json({ content: [{ type: 'text', text }] })` 并断言收到的 `model` / `system` / `messages`。

覆盖：405、空/超长 prompt 400、非法 JSON 400、无服务商 409、正常 200、**走真实 router**、上游 401 带出 status+原文、`content blocks: thinking`、非 JSON body、OpenAI 格式走代理（断言落在 `/chat/completions` 且带 `x-opencode-session`）。

**前端 `desktop/src/components/chat/ChatInput.test.tsx`（75 个用例，新增 4）**

该文件用 `vi.mock('../../api/sessions', ...)` **整体替换模块**——mock 对象里没列出的方法运行时就是 `undefined`。新增 `optimizePrompt` 必须同时改三处：

1. `vi.hoisted` 块加 `optimizePrompt: vi.fn()`
2. `vi.mock` 的对象里加 `optimizePrompt: mocks.optimizePrompt`
3. `beforeEach` 里 `mocks.optimizePrompt.mockResolvedValue({ optimized: '...' })`

用例：回填成功、改写结果与原文相同则不动、空输入禁用、失败弹 toast 且保留原文。

---

## 3. 两个功能共同的坑（血泪版）

| 坑 | 说明 |
|---|---|
| **i18n 只加 en + zh** | 另 4 个语言包是 `Record<TranslationKey, string>` 穷尽类型，少一个直接 `tsc` 失败 |
| **把失败压成 `null`** | 错误信息全丢，用户被指去检查没问题的配置。要返回带详情的判别联合 |
| **`.json()` 代替 `.text()` + parse** | 丢掉原始 body，SSE / 非 JSON 响应无法定位 |
| **React 状态 setter 只传第一个参数** | `MentionComposer` 的 mentions 是 ProseMirror pill 区间，必须显式传 `[]` |
| **正则过度转义** | `[\\]` 对、`[\\\\]` 错；`[\\/]` 对、`[\\\\/]` 错 |
| **`const` 在声明前被使用** | 是 TDZ 运行时错误，不是 lint 提示。安全分级改造时最容易踩 |
| **只调 handler 不调 router 的测试** | 漏注册路由时依然通过，而漏注册 = 功能「不生效」 |

---

## 4. 验证结果（2026-09-19 实测）

| 检查 | 结果 |
|---|---|
| `bun test src/server/__tests__/optimize-api.test.ts` | 11 pass / 0 fail |
| `bun test src/server/__tests__/filesystem.test.ts` | 8 pass / 0 fail |
| `bun test src/server/__tests__/title-service.test.ts` | 16 pass / 1 fail（见下） |
| 服务端全量 `bun test src/server` | 3067 pass / 38 fail —— **与 HEAD 基线逐条一致，零回归** |
| `cd desktop && bun run lint`（eslint + `tsc --noEmit`） | 通过 |
| `cd desktop && bun run test -- --run src/components/chat src/components/composite src/api` | 882 pass / 0 fail |
| `bun test scripts/pr/dead-imports.test.ts scripts/pr/module-graph.test.ts scripts/pr/release-workflow.test.ts` | 65 pass / 0 fail |

**两处已知的失败/未覆盖**：

1. `src/server/__tests__/title-service.test.ts` 的「uses the endpoint and client headers…」在本机 Windows 上失败，原因是 `afterEach` 清理临时目录时后台 trace projection 仍持有句柄，`fs.rm` 报 `EBUSY`——**测试体断言本身通过**。已确认该文件未改动、HEAD 基线同样失败，属既有环境问题。写新的代理类测试时照 `proxy-anthropic-compat.test.ts` 的 teardown 写（`resetSettingsCache` → `drainTraceCaptureForTests` → `clearTraceCaptureStateForTests` → 重试 `fs.rm`）。
2. `filesystem.test.ts` 的符号链接用例在 `win32` 直接 return，**本次对它的断言修改尚未在真实 macOS/Linux 上跑过**，需要在 CI 或对应平台确认。

---

## 5. 手动验证清单

```bash
bun install && cd desktop && bun install
cd desktop && bun run dev
```

**跨盘符目录浏览**

| # | 操作 | 预期 |
|---|---|---|
| 1 | 目录选择器 → 浏览到 `C:\` | 列表顶部出现其他盘符，标题「切换盘符：」；不再渲染死掉的 `..` |
| 2 | 点 `D:\` | 跳到 D 盘根，面包屑用 `\` 分隔 |
| 3 | 输入框敲 `D:\MyProject`，分别按 Enter / 点 Go | 两种方式都能跳转 |
| 4 | 输入一个不存在的路径 | 弹「无法打开该文件夹」toast |
| 5 | 选定目录建会话后做文件搜索 | 正常；未注册目录的**搜索**仍 403 |
| 6 | macOS/Linux 上重复 1–3 | 面包屑用 `/`，不显示盘符分组 |

**提示词优化**

| # | 操作 | 预期 |
|---|---|---|
| 1 | 配好服务商，输入框写「写个登录功能」 | ⚡ 可点击 |
| 2 | 点击 ⚡ | 按钮变 Spinner 且禁用 |
| 3 | 等待返回 | 输入框被替换为更具体的提示词，光标在末尾 |
| 4 | **用 OpenAI 格式中转（DeepSeek 等）重复 1–3** | 同样成功（这是不复用 `providerCompletion` 会失败的场景） |
| 5 | 输入框为空时看按钮 | 禁用 |
| 6 | 未配置服务商时点击 | toast 409「未配置服务商」，输入框不变 |
| 7 | 故意把服务商密钥改错后点击 | toast 带 `HTTP 401` 与上游原文（而不是笼统的「没返回提示词」） |
| 8 | 输入框里有 @ 文件 pill 时点击 | 模型收到路径文本；回填后 pill 变成纯文本 |

---

## 6. 已知限制

1. **映射网络驱动器**（`Z:` → SMB 共享）与本地盘符在路径形态上无法区分，`containsVulnerableUncPath` 拦不住。
2. **H5 远程可枚举全盘目录**（见 §1.2 的说明），本次按决策不加调用方限制。
3. **⚡ 只加在 `ChatInput`**，首页空会话的 `EmptySession.tsx` 是另一份 composer 实现，未加。
4. `getWindowsDriveEntries` 用同步 `statSync` 逐个探测 C–Z。断连的映射盘符可能让这一步卡顿（仅在盘符根目录发生）。
5. 提示词优化**没有做请求去重/取消**：连点会并发多次上游调用（按钮在加载中已禁用，正常操作路径下不会触发）。
